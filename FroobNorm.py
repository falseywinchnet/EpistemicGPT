#MIT License Terms Apply
#joshuah.rainstar@gmail.com 2026 if it works for you tell a friend

'''
⢕⢕⢕⢑⠁⢁⣼⣿⣻⢿⣟⢿⡻⡙⡏⡏⣟⡽⡹⣹⢪⡻⢽⡫⠯⣻⢛⢿⣻⣿⣿⡿⣧⢍⠝⡍⡏⡎⡕⢵
⢕⢕⠸⡐⣠⣿⣗⢯⡺⡽⡸⡅⠕⢕⢐⠑⢕⠸⢙⢕⢱⢸⠸⢜⠴⡈⡜⡔⢅⢟⢞⣿⣿⣧⠨⡊⣜⢜⢜⡵
⢕⢌⢂⢀⣿⡷⡧⣯⡣⡹⡎⠪⣈⡂⣦⣁⣱⡱⣱⣡⢑⣘⡨⢮⢝⢞⣎⣖⣵⢳⣸⣯⣿⣿⣇⢣⢪⡪⣣⣻
⢕⢐⠄⣸⣿⣿⡝⣮⡪⡪⣊⢎⡔⣗⢳⢪⢻⠾⡻⡿⡻⣗⢾⢗⡿⢿⢻⡻⡾⣟⣶⣯⣿⣿⡇⡎⢆⢇⢇⢿
⢕⠐⠄⣿⣿⣿⡽⢜⡲⣇⣷⡛⢗⢙⠹⠘⠔⠑⠉⠘⠜⠑⠔⠑⢘⠈⠂⠊⠘⠈⠈⠘⠹⢿⡧⡣⡣⡣⡳⡽
⢅⠅⠄⣿⣿⣿⢝⢷⣻⡻⠁⠄⠄⠄⠄⢀⠄⠄⠄⠄⠂⠬⡀⡄⠄⠄⠄⡀⠄⠄⠄⠄⠁⠂⢻⣕⢧⢳⢕⣿
⠅⠂⠄⢻⣿⣯⠧⢟⣿⣔⠄⠁⡀⡀⠄⠄⠄⣀⠄⠄⡀⠩⠳⠵⠁⠄⡀⠄⢰⣁⠄⠠⠄⠄⠈⢿⣪⡺⡜⣾
⢵⣷⠶⣼⠏⣿⠹⡙⠄⠁⠄⠖⠂⠄⡀⠄⠄⠹⣾⠄⠄⢔⣻⣿⡳⠄⠄⠄⠈⡷⠠⠄⠄⠄⠄⣼⡟⠵⡭⣻
⡪⡂⡕⡄⢠⣿⣏⢌⢠⠰⡌⡢⠄⠄⠅⠠⠄⠐⠄⠄⡔⣵⢾⣟⣯⢧⢂⢠⢀⠄⠄⠄⣀⢤⢾⢦⣿⣇⢗⣿
⠜⣔⠄⠈⢘⣿⣯⡖⠔⠑⢜⢐⠁⢂⠄⠢⢐⠐⢀⢕⢝⡚⡟⠿⡿⣿⣪⡢⠳⣭⣊⢆⠣⠣⠣⠓⡯⢷⡣⣿
⠝⣾⠄⠄⢸⣹⣷⣆⢀⢑⢐⠰⢈⢐⠨⠨⠠⠐⡎⠫⠑⢌⠌⡈⡈⠪⠚⠷⠈⠰⢕⢟⢎⢎⠂⠌⣾⣿⢸⣺
⢅⠱⢷⢄⡀⢽⣿⣟⡀⠑⡐⢅⢃⠢⠑⠅⢅⢢⠈⠄⠄⠄⠁⠐⠈⢀⢀⢬⢤⢠⢌⠪⠲⢈⠸⡠⣿⢾⠱⣽
⡅⠄⠳⣝⠄⢸⣿⣻⡜⠆⠌⣐⡂⢅⠅⡍⡆⢧⢑⢦⠕⠔⠤⠤⠤⠳⢯⣯⢿⢵⠥⡧⣑⢄⠃⣆⣿⠃⢑⢵
⢆⠕⡠⡉⠃⠸⣿⣿⡸⠰⢁⢐⠄⡥⡪⠢⡸⡸⢊⠁⠄⠡⠡⠨⠐⠄⠈⠈⠫⡇⠟⣜⣖⡔⠥⣢⣻⠄⠄⡸
⡣⡣⡱⡨⠢⡀⢹⡗⡳⡉⢐⠄⠕⡽⢐⠁⠄⠅⠐⠈⠄⠄⠄⢀⢀⣀⣀⣀⠄⠁⠄⠨⣾⣗⡘⣆⣿⠄⠐⡌
⡪⡪⡢⡣⡃⢆⠸⣿⡢⡸⠂⡘⡩⠼⡄⢄⠰⡐⢔⠑⢍⢚⢙⢊⠣⠓⠝⢚⢹⢕⢕⢡⠺⡒⠕⠅⡾⣤⣐⢜
⡕⢕⢸⢨⢊⠆⡂⡹⣯⣪⡀⡄⢜⡽⡀⢢⠊⡊⡢⢑⢀⠄⡀⠄⠄⡀⠄⡊⠮⡪⡊⢆⢆⠯⢨⢢⠁⠄⣿⣽
⢎⢕⢕⢅⢇⣣⡱⠒⢿⡜⣖⡂⢚⡺⣎⢐⠄⡊⡐⠰⡀⡅⣀⡢⣐⢔⡰⡨⡘⡄⡣⡡⣏⡓⠇⠁⠄⠄⣍⣿
⡣⡱⡡⡣⣳⠞⠄⠄⠸⣿⣝⢮⣶⢪⡗⡄⠠⢂⠌⠆⡅⠇⠇⡓⡘⡘⡪⣚⠪⡪⡘⡜⡂⢪⡃⠄⠄⢀⣽⢿
⡕⢕⢕⢽⠁⠄⠄⠄⠄⡑⠌⠹⢹⢹⢭⢻⡅⡆⡈⡠⠠⠁⡂⠄⠄⠠⢀⠂⠅⡃⠅⠁⡄⡣⡂⠄⢠⢙⢄⢾
⡕⡕⢵⠃⠄⢀⠄⠡⠄⢈⢂⢀⠄⠁⠌⠈⠪⠒⠢⠐⡐⠡⠐⠠⠡⠈⠄⠌⠄⠄⢀⠌⡀⠊⢐⡴⠩⡓⠔⢝
⢎⢪⢸⠄⠄⠘⠐⠄⠄⠄⠐⡀⠌⡀⠄⢂⠠⠄⠄⠁⠄⠁⠄⠄⠄⠄⠄⠄⠄⠄⡀⠄⡠⠔⠁⠐⠐⡭⡨⡘
⢕⠅⢝⣕⠄⠄⠄⠄⠄⠄⠄⠐⠄⠄⠄⠠⠄⠄⠄⠄⠄⠄⠄⠄⠄⠄⠄⠄⡀⠅⠐⠁⠄⠄⠈⢀⠁⡒⡊⠌
⡕⣌⣄⣿⣾⣄⠄⠄⠄⠄⠄⠄⠂⠁⠄⠄⠄⠄⠄⠄⠄⠄⠄⢀⠄⠄⠈⠄⠄⠄⠄⠄⠄⠈⠄⠄⠄⢂⠠⠠
What if I told you that you could norm during training, and then stop norming during eval and still have the same behavior,
because you used a norm that gets baked into the weights as a conditioning as opposed to a lazy constrainer,
and thus skip the entire norm operation during evaluation, and do this for all norms in your model?
Take the FrobPill. 

For a vector, the operator norm is exactly equal to the Euclidean (L2) norm, and the only "singular value" is the magnitude of that vector.
Your must accept the Euclidean Ball Projection. the geometry is isotropic. 


'''


class AnchorPenaltyInjector(torch.autograd.Function):
    @staticmethod
    def forward(ctx, x, delta, radius, eta, penalty_lambda):
        ctx.save_for_backward(delta, radius)
        ctx.eta = eta
        ctx.penalty_lambda = penalty_lambda
        return x

    @staticmethod
    def backward(ctx, grad_output):
        delta, radius = ctx.saved_tensors
        violation = torch.relu(radius - ctx.eta)
        # Gradient points inwards. 
        # Added small epsilon to denominator to prevent NaN on perfect zeros
        grad_penalty = (2 * ctx.penalty_lambda * violation / (radius + 1e-6)) * delta
        return grad_output + grad_penalty, None, None, None, None


#apply frobheadnorm like so:
'''
        self.q_proj = nn.Linear(n_embd, n_embd, bias=False)
        self.v_proj = nn.Linear(n_embd, n_embd, bias=False)
        self.o_proj = nn.Linear(n_embd, n_embd, bias=False)
        
        nn.init.eye_(self.q_proj.weight) 
        
        self.skew_basis = nn.Parameter(torch.randn(n_heads, head_dim, head_dim) * 0.01)
        self.rope = RoPE(head_dim)
        
        # --- Configurable Norms on Q and V ---
        self.q_norm = FrobNorm(self.n_heads,head_dim, penalty_lambda=1.0)
        self.v_norm = FrobNorm(self.n_heads,head_dim, penalty_lambda=1.0)


    def get_orthogonal_matrix(self):
        skew = self.skew_basis - self.skew_basis.transpose(-1, -2)
        return torch.matrix_exp(skew)

    def forward(self, x, force_norm_off=False):
        B, T, C = x.shape
        H, D = self.n_heads, self.head_dim

        Rs = self.get_orthogonal_matrix()
        W_q = self.q_proj.weight.view(H, D, C)
        W_k_heads = Rs @ W_q
        W_k = W_k_heads.reshape(H * D, C)
        
        q = self.q_proj(x).view(B, T, H, D)
        k = F.linear(x, W_k).view(B, T, H, D)
        v = self.v_proj(x).view(B, T, H, D)


        q = self.q_norm(q)
        # K is never normed or tampered with
        #it generates derived from q to begin with, so conditioning on Q impacts conditioning on K
        
        # Apply RoPE
        q = self.rope(q)
        k = self.rope(k)


and later :

        out = out.transpose(1, 2).contiguous() # B,T,H,D
        out = self.v_norm(out)
        out = out.view(B, T, C)
        return self.o_proj(out)
        
'''
class FrobHeadNorm(nn.Module):
    def __init__(self, n_heads, head_dim):
        super().__init__()
        self.eta = head_dim #seems to be correct, unclear? 16 worked for head dim of 16
        # Shape: (1, 1, n_heads, head_dim) for easy broadcasting with [B, T, H, D]
        self.anchor = nn.Parameter(torch.zeros(1, 1, n_heads, head_dim))

    def forward(self, x):
        # x is [B, T, H, D]
        delta = x - self.anchor
        # Norm is taken across the head_dim (D)
        radius = torch.norm(delta, p=2, dim=-1, keepdim=True) + 1e-6
        
        if self.training:
            x_conditioned = AnchorPenaltyInjector.apply(
                x, delta, radius, self.eta, 1.0
            )
            scale = torch.clamp(self.eta / radius, max=1.0)
            return self.anchor + ((x_conditioned - self.anchor) * scale)
        else:
            return x



#apply froobnorm + residual highway in your blocks like so:
#y = x + attn(frob1(x)) #this is an additive update. don't pretend it isnt.
#x = frobnorm3(x*F.sigmoid(self.gamma*x) + mlp(frob2(y)) #this is a janitor. Don't pretend it isnt.
class FrobNorm(nn.Module):
    def __init__(self,dim):
        super().__init__()
        self.eta = dim #seems to be correct, unclear? 16 worked for head dim of 16
        # Shape: (1, 1, n_heads, head_dim) for easy broadcasting with [B, T, C]
        self.anchor = nn.Parameter(torch.zeros(1, 1, dim))

    def forward(self, x):
        # x is [B, T, C]
        delta = x - self.anchor
        # Norm is taken across the dim (C)
        radius = torch.norm(delta, p=2, dim=-1, keepdim=True) + 1e-6
        
        if self.training:
            x_conditioned = AnchorPenaltyInjector.apply(
                x, delta, radius, self.eta, 1.0
            )
            scale = torch.clamp(self.eta / radius, max=1.0)
            return self.anchor + ((x_conditioned - self.anchor) * scale)
        else:
            return x
